{
  "name": "ebirforms-0",
  "private": true,
  "type": "module",
  "description": "Native eBIRForms reconstruction with a Zig calendar engine and SQLite policy store.",
  "scripts": {
    "generate": "npm run generate:tax-catalog && node scripts/flatten-native.mjs",
    "generate:tax-catalog": "node --experimental-strip-types scripts/tax-catalog/generate.ts",
    "check:tax-catalog": "npm run typecheck:tax-catalog && npm run check:field-ownership-manifests && node --experimental-strip-types scripts/tax-catalog/generate.ts --check",
    "check:field-ownership-manifests": "node --experimental-strip-types scripts/tax-catalog/manifests/check.ts",
    "typecheck:tax-catalog": "tsc --noEmit -p tsconfig.tax-catalog.json"
  },
  "//": "--experimental-strip-types is required on Node 22.15, the documented minimum. Node 22.18+ and 23.6+ strip types by default and accept the flag as a no-op, so one invocation covers every supported runtime.",
  "engines": {
    "node": ">=22.15"
  },
  "devDependencies": {
    "@native-sdk/cli": "0.6.1",
    "@types/node": "24.13.3",
    "typescript": "6.0.3"
  }
}
