<!-- GENERATED; edit fragments and run npm run generate. -->
<import src="app-shared.generated.native"/>
<import src="app-pages.generated.native"/>
<import src="app-forms.generated.native"/>
<import src="app-auxiliary.generated.native"/>
<stack grow="1"><if test="{shellVisible}"><use template="o" app_icon="{appIcon}" bagong_logo="{bagongPilipinasLogo}" bir_logo="{birLogo}" goldcoders_logo="{goldcodersLogo}"><column grow="1"><if test="{contentPage == 'global_dashboard'}"><use template="z"/></if><if test="{contentPage == 'taxpayer_dashboard'}"><use template="bl"/></if><if test="{contentPage == 'profile_setup'}"><use template="be"/></if><if test="{contentPage == 'tax_form_profile'}"><use template="bj"/></if><if test="{contentPage == 'import_data'}"><use template="bn"/></if><if test="{contentPage == 'background_tasks'}"><use template="bs"/></if><if test="{contentPage == 'tax_calendar'}"><use template="by"/></if><if test="{contentPage == 'settings'}"><use template="cd"/></if><if test="{contentPage == 'screen_gallery'}"><use template="ci"/></if><if test="{contentPage == 'form_0605'}"><use template="cp"/></if><if test="{contentPage == 'form_0619_e'}"><use template="cw"/></if><if test="{contentPage == 'form_0619_f'}"><use template="dd"/></if><if test="{contentPage == 'form_1601_c'}"><use template="dl"/></if><if test="{contentPage == 'form_1601_eq'}"><use template="dt"/></if><if test="{contentPage == 'form_1701'}"><use template="em"/></if><if test="{contentPage == 'form_1701q'}"><use template="ev"/></if><if test="{contentPage == 'form_1702_rt'}"><use template="fp"/></if><if test="{contentPage == 'form_1702_mx'}"><use template="ff"/></if><if test="{contentPage == 'form_2550q'}"><use template="gb"/></if><if test="{contentPage == 'form_2551q'}"><use template="gk"/></if></column></use></if><if test="{contentPage == 'taxpayer_dashboard'}"><if test="{profileDeadlineDialogOpen}"><column grow="1" padding="16" main="center" cross="center" label="Dismiss dialog" on-press="profile_deadline_close_dialog"><dialog width="{profileDeadlineDialogWidth}" on-dismiss="profile_deadline_close_dialog"><use template="an"/></dialog></column></if><if test="{profileFormInfoDialogOpen}"><column grow="1" padding="16" main="center" cross="center" label="Dismiss dialog" on-press="profile_forms_close_info"><dialog width="{profileFormInfoDialogWidth}" on-dismiss="profile_forms_close_info"><use template="ao" code="{profileFormInfoCode}" title="{profileFormInfoTitle}" category="{profileFormInfoCategory}" cadence="{profileFormInfoCadence}" capability="{profileFormInfoCapability}" revision="{profileFormInfoRevision}"/></dialog></column></if></if><if test="{profileDirtyNavigationVisible}"><column grow="1" padding="16" main="center" cross="center" label="Keep editing" on-press="profile_keep_editing"><dialog width="{profileDirtyNavigationDialogWidth}" height="{profileDirtyNavigationDialogHeight}" on-dismiss="profile_keep_editing"><use template="bc"/></dialog></column></if><if test="{page == 'aux_lock_screen'}"><use template="gm"/></if><if test="{page == 'aux_profile_auth'}"><use template="go"/></if><if test="{page == 'aux_admin_auth'}"><use template="gq"/></if><if test="{page == 'aux_command_palette'}"><use template="gs"/></if><if test="{page == 'aux_html_preview'}"><use template="gv"/></if><if test="{page == 'aux_email_confirmation'}"><use template="gx"/></if><if test="{page == 'aux_debug_log'}"><use template="gy"/></if></stack>
